# Frontend
React with Vite and Bootstrap. Run `npm ci`, `npm run dev`, or `npm run build`. Development uses port 3000 and proxies `/api` to backend port 4000. Shared Header, Sidebar, Footer and Layout components are in src/components. No EJS templates.
